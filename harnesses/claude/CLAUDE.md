@RTK.md

# File operations
- To copy a file, use `cp` via Bash — never read and rewrite it.
- To move or rename, use `mv`.
- Prefer Edit over Write for modifications; only use Write for genuinely new files or complete rewrites where Edit would require replacing the entire content anyway.
