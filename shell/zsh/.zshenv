export PATH="${HOME}/.local/bin:$PATH"

# shortcut to this dotfiles path is $DOTFILES
export DOTFILES="$HOME/.dotfiles"

# Extra completions folder
export ZSH_EXTRA_COMPLETIONS="$DOTFILES/shell/zsh/completions"

# editors
export EDITOR='vim'
export VEDITOR='code'
